// Global polyfills to support legacy browsers like iOS 12 (Safari 12)
// Note: Order matters. These are safe to include multiple times.
import 'core-js/stable';
import 'regenerator-runtime/runtime';
import 'whatwg-fetch';
import 'fast-text-encoding';

export {};
